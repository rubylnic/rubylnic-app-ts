describe('When given 2 numbers', () => {
    it('returns the sum of those 2 numbers', () => {
        const result = 15;
        const expected = 15

        expect(result).toEqual(expected)
    })
})